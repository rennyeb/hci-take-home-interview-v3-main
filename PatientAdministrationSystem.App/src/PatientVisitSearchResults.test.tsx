
//TODO rename everything
import { render, screen } from '@testing-library/react';
// import Greeting from './Greeting';

describe('Greeting Component', () => {
  it('renders the greeting message with the provided name', () => {
    // // Arrange: Render the component with a name prop
    // render(<Greeting name="John" />);

    // // Act: Query the rendered content
    // const greetingMessage = screen.getByText('Hello, John!');

    // // Assert: Check if the greeting message is in the document
    // expect(greetingMessage).toBeInTheDocument();

    throw "bad"
    console.log("hi")
  });

  it('renders the correct greeting when another name is passed', () => {
    // // Arrange: Render the component with a different name prop
    // render(<Greeting name="Jane" />);

    // // Act: Query the rendered content
    // const greetingMessage = screen.getByText('Hello, Jane!');

    // // Assert: Check if the greeting message is in the document
    // expect(greetingMessage).toBeInTheDocument();
  });
});
